import pathManager from './methods';

// const nameAndPathMap = {
// 	example: '/example/{exampleId}/{slug}',
// 	noParams: '/no-params',
// }

// const { getPath } = pathManager(nameAndPathMap)

// console.log(getPath('example', { exampleId: 1, slug: 123 }))

export default pathManager;
